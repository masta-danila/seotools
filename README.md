# SEO Tools - Автоматическая генерация метатегов

Система автоматической генерации H1, Title и Description для сайтов на основе анализа конкурентов и LLM.

## Возможности

- 📊 Чтение данных из Google Sheets
- 🔍 Поиск конкурентов через Arsenkin API
- 🏷️ Парсинг метатегов конкурентов
- 🧠 Лемматизация текстов (извлечение ключевых слов)
- 🤖 Генерация метатегов через LLM (Claude/OpenAI/Gemini)
- ✅ Автоматическая загрузка результатов в Google Sheets
- 📝 Логирование всех операций
- ♻️ Бесконечный цикл работы с настраиваемым интервалом

## Быстрый старт

### 1. Клонирование репозитория
```bash
git clone https://github.com/masta-danila/seotools.git
cd seotools
```

### 2. Создание виртуального окружения
```bash
python3 -m venv venv
source venv/bin/activate  # Для Linux/Mac
# или
venv\Scripts\activate  # Для Windows
```

### 3. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 4. Настройка окружения

Создайте файл `.env` в корне проекта:
```bash
# API ключи
ANTHROPIC_API_KEY=your_anthropic_key
OPENAI_API_KEY=your_openai_key
GOOGLE_API_KEY=your_google_key
ARSENKIN_API_KEY=your_arsenkin_key
DEEPSEEK_API_KEY=your_deepseek_key
GROK_API_KEY=your_grok_key
```

### 5. Настройка Google Sheets

#### 5.1. Получите credentials.json:
1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите Google Sheets API и Google Drive API
4. Создайте Service Account
5. Скачайте JSON ключ
6. Сохраните как `gsheets/credentials.json`

#### 5.2. Создайте sheets_config.json:
```json
{
  "spreadsheet_ids": [
    "YOUR_SPREADSHEET_ID_1",
    "YOUR_SPREADSHEET_ID_2"
  ]
}
```

### 6. Запуск

```bash
python main.py
```

## Структура проекта

```
seotools/
├── arsenkin/              # Модули работы с Arsenkin API
│   ├── search_parser.py   # Поиск конкурентов
│   ├── search_batch_processor.py
│   ├── h_parser.py        # Парсинг метатегов
│   └── blacklist_domains.json
├── gsheets/               # Модули работы с Google Sheets
│   ├── sheets_reader.py   # Чтение данных
│   ├── sheets_updater.py  # Обновление данных
│   ├── credentials.json   # (не в git)
│   └── sheets_config.json # (не в git)
├── lemmatizers/           # Лемматизация текстов
│   ├── lemmatizer.py
│   └── lemmatizer_processor.py
├── metagenerators/        # Генерация метатегов через LLM
│   ├── metagenerator.py
│   └── metagenerator_batch.py
├── llm/                   # LLM клиенты
│   ├── llm_router.py
│   ├── claude_request.py
│   ├── gpt_request.py
│   └── gemini_request.py
├── logs/                  # Логи (не в git)
├── jsontests/             # Временные JSON данные (не в git)
├── main.py                # Главный оркестратор
├── logger_config.py       # Настройка логирования
└── requirements.txt       # Зависимости

```

## Использование

### Основной пайплайн
```bash
python main.py
```

Выполняет полный цикл:
1. Чтение Google Sheets
2. Поиск конкурентов
3. Парсинг метатегов
4. Лемматизация
5. Генерация через LLM
6. Загрузка в Google Sheets

### Отдельные модули

#### Чтение Google Sheets
```bash
python gsheets/sheets_reader.py
```

#### Поиск конкурентов
```bash
python arsenkin/search_batch_processor.py
```

#### Парсинг метатегов
```bash
python arsenkin/h_parser.py
```

#### Лемматизация
```bash
python lemmatizers/lemmatizer_processor.py
```

#### Генерация метатегов
```bash
python metagenerators/metagenerator_batch.py
```

## Настройка параметров

Параметры в `main.py`:

```python
# Интервал между циклами
SLEEP_MINUTES = 10  # минут

# Шаг 2: Поиск конкурентов
max_concurrent=2        # Одновременных запросов
wait_per_query=15       # Интервал проверки статуса

# Шаг 4: Лемматизация
title_min_words=4
title_max_words=6
description_min_words=6
description_max_words=10

# Шаг 5: Генерация метатегов
model="claude-sonnet-4-5-20250929"
max_concurrent=2
max_retries=3
```

## Логирование

Логи сохраняются в папке `logs/`:
- `pipeline.log` - главный оркестратор
- `search.log` - поиск конкурентов
- `parser.log` - парсинг метатегов
- `lemmatizer.log` - лемматизация
- `metagenerator.log` - генерация метатегов
- `sheets_reader.log` - чтение Google Sheets
- `sheets_updater.log` - обновление Google Sheets

### Просмотр логов в реальном времени
```bash
tail -f logs/pipeline.log
```

## Развертывание на сервере

Подробная инструкция в [DEPLOY.md](DEPLOY.md)

Быстрый деплой:
```bash
./deploy_server.sh
./setup_systemd_service.sh
```

## Требования

- Python 3.11+
- API ключи:
  - Arsenkin API (обязательно)
  - Anthropic Claude / OpenAI / Google Gemini (один на выбор)
  - Google Service Account для Sheets (обязательно)

## Rate Limits

### Arsenkin API
- Максимум 30 запросов в минуту
- Максимум 5 задач одновременно
- Автоматические паузы между шагами для соблюдения лимитов

### LLM API
- Настройте в зависимости от вашего плана
- Используйте `max_concurrent` для ограничения

## Лицензия

MIT

## Автор

Danila Dzhaev
